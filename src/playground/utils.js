console.log('utils is RUNNING!');

export const square = (x) => x*x;

export const add = (x,y) => x + y;

export default  (x,y) => x-y;

// export default substract;  THIS WORKS, or use the short way export default  (x,y) => x-y;
//export {square,add, subsctract as default};